CREATE TABLE product(id int, brand VARCHAR(150), product_name VARCHAR(150), price NUMERIC, website_link VARCHAR(150), tags VARCHAR(150), image_link VARCHAR, product_description VARCHAR(4000), product_type VARCHAR(150), product_colors text[][]);
